name: Shellcheck
on:
  pull_request:
    paths:
      - "rhtas/*.sh"
    branches: [release-1.4]

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: shellcheck rhtas/*.sh
